export class Song {
    constructor(title,artist,album,genre,length,songURL,imageURL){
         this.title=title;
         this.artist=artist;
         this.album=album;
         this.genre=genre;
         this.length=length;
         this.songURL=songURL;
         this.imageURL=imageURL;
    } 
}